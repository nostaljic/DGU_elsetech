import 'package:flutter/material.dart';
import 'package:dgu_elsetech/screen/body.dart';

class RequestScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(title: 'body',),
    );
  }
}